---
const today = new Date();
---

<footer>
	<div class="footer-content">
		<div class="brand-section">
			<h3 class="brand-name">Synthwave</h3>
			<p class="brand-tagline">AI-Powered Insights & Creative Tech Adventures</p>
		</div>
		
		<div class="social-section">
			<p class="connect-text">Let's Connect!</p>
			<div class="social-links">
				<a href="https://twitter.com/siyabuilds" target="_blank" class="social-link">
					<span class="sr-only">Follow siyabuilds on Twitter/X</span>
					<i class="fab fa-twitter" aria-hidden="true"></i>
				</a>
				<a href="https://linkedin.com/in/siyabuilds" target="_blank" class="social-link">
					<span class="sr-only">Connect with siyabuilds on LinkedIn</span>
					<i class="fab fa-linkedin" aria-hidden="true"></i>
				</a>
				<a href="https://github.com/siyabuilds" target="_blank" class="social-link">
					<span class="sr-only">Follow siyabuilds on GitHub</span>
					<i class="fab fa-github" aria-hidden="true"></i>
				</a>
			</div>
		</div>
	</div>
	
	<div class="footer-bottom">
		<div class="footer-left">
			<p class="copyright">&copy; {today.getFullYear()} Siyabonga Lukhele (siyabuilds). All rights reserved.</p>
			<a href="https://github.com/siyabuilds/ai_powered_devblog" target="_blank" class="repo-link">
				<span class="sr-only">View this project on GitHub</span>
				<i class="fab fa-github" aria-hidden="true"></i>
				View Repository
			</a>
		</div>
		<p class="powered-by">Powered by AI & Human Creativity</p>
	</div>
</footer>
<style>
	footer {
		background: linear-gradient(135deg, rgba(var(--purple-100), 0.8), rgba(var(--blue-100), 0.6));
		border-top: 1px solid rgba(var(--purple-200), 0.4);
		margin-top: 4rem;
		padding: 3rem 2rem 2rem;
		backdrop-filter: blur(10px);
	}
	
	.footer-content {
		max-width: 960px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr auto;
		gap: 3rem;
		align-items: center;
	}
	
	.brand-section {
		text-align: left;
	}
	
	.brand-name {
		font-size: 1.8rem;
		font-weight: 700;
		color: var(--primary-dark);
		margin: 0 0 0.5rem 0;
		background: linear-gradient(135deg, var(--primary), var(--accent));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}
	
	.brand-tagline {
		margin: 0;
		color: rgba(var(--gray-dark), 0.7);
		font-size: 0.95rem;
		font-weight: 500;
	}
	
	.social-section {
		text-align: center;
	}
	
	.connect-text {
		margin: 0 0 1rem 0;
		color: rgba(var(--gray-dark), 0.7);
		font-size: 0.9rem;
		font-weight: 500;
		text-transform: uppercase;
		letter-spacing: 0.5px;
	}
	
	.social-links {
		display: flex;
		gap: 1rem;
		justify-content: center;
	}
	
	.social-link {
		display: flex;
		align-items: center;
		justify-content: center;
		width: 48px;
		height: 48px;
		background: rgba(255, 255, 255, 0.8);
		border: 1px solid rgba(var(--purple-200), 0.5);
		border-radius: 50%;
		color: rgba(var(--gray-dark), 0.7);
		text-decoration: none;
		transition: all 0.3s ease;
		backdrop-filter: blur(5px);
		font-size: 20px;
	}
	
	.social-link:hover {
		background: rgba(var(--purple-100), 0.9);
		border-color: var(--accent);
		color: var(--accent);
		transform: translateY(-2px);
		box-shadow: 0 8px 16px rgba(var(--purple-900), 0.15);
	}
	
	.footer-bottom {
		max-width: 960px;
		margin: 0 auto;
		padding-top: 2rem;
		margin-top: 2rem;
		border-top: 1px solid rgba(var(--purple-200), 0.3);
		display: flex;
		justify-content: space-between;
		align-items: center;
		flex-wrap: wrap;
		gap: 1rem;
	}
	
	.footer-left {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}
	
	.copyright {
		margin: 0;
		color: rgba(var(--gray-dark), 0.7);
		font-size: 0.85rem;
	}
	
	.repo-link {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.5rem 1rem;
		background: rgba(var(--gray-dark), 0.8);
		border: 1px solid rgba(var(--gray-dark), 0.9);
		border-radius: 6px;
		color: rgba(255, 255, 255, 0.9);
		text-decoration: none;
		font-size: 0.85rem;
		font-weight: 500;
		transition: none;
		width: fit-content;
	}
	
	.repo-link i {
		font-size: 16px;
	}
	
	.powered-by {
		margin: 0;
		color: rgba(var(--gray-dark), 0.6);
		font-size: 0.8rem;
		font-style: italic;
		background: linear-gradient(90deg, var(--accent), var(--secondary));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}
	
	@media (max-width: 720px) {
		footer {
			padding: 2rem 1rem 1.5rem;
		}
		
		.footer-content {
			grid-template-columns: 1fr;
			gap: 2rem;
			text-align: center;
		}
		
		.brand-section {
			text-align: center;
		}
		
		.footer-bottom {
			flex-direction: column;
			align-items: center;
			gap: 1rem;
		}
		
		.footer-left {
			text-align: center;
			align-items: center;
		}
		
		.social-link {
			width: 44px;
			height: 44px;
			font-size: 18px;
		}
	}
</style>
